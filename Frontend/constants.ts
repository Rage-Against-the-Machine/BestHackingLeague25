export const API_URL = 'http://100.82.90.77:6969';

import { Product, ProductCategory } from './types';

// The application now starts with an empty state managed by services/productService.ts
// Real data is created by the user in the Store Panel.
export const MOCK_PRODUCTS: Product[] = [];
